# React Router Decision
We use React Router v6 to manage navigation between the different operational views (Dashboard, Daily Entry, etc.). A `RootLayout` component was created to hold the sidebar navigation, ensuring it remains persistent across route changes.
