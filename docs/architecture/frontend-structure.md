# Frontend Structure
The application follows a standard Vite + React + TS architecture:
- React Router manages navigation.
- A RootLayout wraps pages with a persistent sidebar and header.
- Tailwind CSS is used for styling.
- Global constants are stored in `src/utils/constants.ts`.
